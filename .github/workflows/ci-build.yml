name: "CI Build"
on : "push"
jobs:
  Build:
    runs-on: "ubuntu-latest"
    steps:
      - name: Install Packages
        run: pip install -r requirements.txt

      - name: Deploying
        run: echo "Deploying ..."